<div class="opp-perf">
<div class="secu">
    <div class="u-fr">
      <div class="frna">
        <div class="frbien"><i class="fi fi-rr-thumbtack"></i> <h1>Bienvenido al perfil de</h1></div>
        <div class="frname"><i class="fi fi-rr-user"></i> <h1>{USERNAME}</h1></div>
      </div>
    </div>
    <div class="tripart">
	<div class="tripm mensajes">
          <dfn>Mensajes</dfn> <span class="valor"></span>
	</div>
	<div class="tripm registro">
          <dfn>Registro</dfn> <span class="valor"></span>
	</div>
	<div class="tripm ultima">
  	{LAST_VISIT_DATE}
	</div>
        <div class="tripm estado">
      	<!-- BEGIN switch_show_status -->
	{USER_ONLINE}
	<!-- END switch_show_status -->
        </div>
    </div>

</div>
<div class="secd">
  <div class="secava">
    <div class="perfilavatar" title="300 x 500" data-bs-toggle="tooltip" data-bs-custom-class="custom-tooltip">{AVATAR_IMG}
      </div>  
  </div>
  <div class="secdos">
    <div class="facgrup">
      <div class="facci">
        <!-- BEGIN switch_groups_list -->
				<!-- BEGIN group_details -->
				<div id="{switch_groups_list.group_details.GROUP_ID}-name"><a href="{switch_groups_list.group_details.U_GROUP}" >{switch_groups_list.group_details.L_GROUP_NAME}</a></div>
				<!-- END group_details -->
		<!-- END switch_groups_list -->
        <script>
        $(document).ready(function(){
  $('.facci').each(function(){
      $(this).find('a:gt(0)').hide();
  });
});
        </script>
      </div>
    <div class="rangofaccion"></div>
    </div>
    <div class="datgen">
      <h1>Datos generales <i class="fi fi-rr-list"></i></h1>
      <div class="tdatge">
        <div class="pedad"><dfn><i class="game-icon game-icon-ages"></i>Edad</dfn> <span class="valor"></span></div>
        <div class="praza"><dfn><i class="game-icon game-icon-angel-wings"></i>Raza</dfn> <span class="valor"></span></div>
        <div class="palt"><dfn><i class="game-icon game-icon-body-height"></i>Altura</dfn> <span class="valor"></span></div>
        <div class="pisl"><dfn><i class="game-icon game-icon-island"></i>Isla Natal</dfn> <span class="valor"></span></div>
        <div class="pberr"><dfn><i class="game-icon game-icon-ages"></i>Berrys</dfn> <span class="valor"></span></div>
        <div class="paku"><dfn><i class="game-icon game-icon-raspberry"></i>Akuma no mi</dfn> <span class="valor"></span></div>
        <div class="pwant"><dfn><i class="game-icon game-icon-wanted-reward"></i>Wanted</dfn> <span class="valor"></span></div>
        <div class="pb"><dfn><i class="game-icon game-icon-drama-masks"></i>PB</dfn> <span class="valor"></span></div>    
      </div>
    </div>
  </div>
  <div class="sectres">
    <div class="fra">
      <dfn>Frase <i class="fi fi-rr-message-quote"></i></dfn>
      <span class="valor"></span>
    </div>   
  <div class="fralink">
      <h1>Contacto y Links <i class="fi fi-rr-link"></i></h1>
      <div class="blinnky">
        	<!-- BEGIN contact_field -->
		<dl id="field_id{contact_field.ID}">
                  {contact_field.CONTENT}
		</dl>
		<!-- END contact_field -->
            		<!-- BEGIN profile_field -->
		<div id="field_id{profile_field.ID}">
                  <!-- BEGIN profil_type_user_posts --> 
				<!-- BEGIN links -->
				<a rel="nofollow" href="/sta/{PUSERNAME}" title="Temas" data-bs-toggle="tooltip" data-bs-custom-class="custom-tooltip"><i class="fi fi-rr-browser"></i></a>
				<a rel="nofollow" href="/spa/{PUSERNAME}" title="Mensajes" data-bs-toggle="tooltip" data-bs-custom-class="custom-tooltip"><i class="fi fi-rr-document"></i></a>
				<!-- END links -->
				<!-- END profil_type_user_posts -->
          	</div>
		<!-- END profile_field -->
    </div>
  </div>
    <div class="music" title="Puedes colocar aquí el listado de canciones que representan a tu personaje, puedes incluir hasta 20 canciones." data-bs-toggle="tooltip" data-bs-custom-class="custom-tooltip">
      <dfn>Mi Playlist <i class="game-icon game-icon-music-spell"></i></dfn>
      <span class="valor"></span></div>
  </div>
</div>
<div class="sect">
  <div class="belpart">
    <h1><i class="game-icon game-icon-sword-spin"></i>Puntos y atributos</h1>
    <div class="bellvl">Nivel <span class="valor"></span></div>
  </div>
  <div class="pponter">
    <div class="PAcampo" title="Puntos de Atributos" data-bs-toggle="tooltip" data-bs-custom-class="custom-tooltip">PA <span class="valor"></span></div>
    <div class="PPcampo" title="Puntos de poder" data-bs-toggle="tooltip" data-bs-custom-class="custom-tooltip">PP <span class="valor"></span></div>
    <div class="PRcampo" title="Puntos de rol" data-bs-toggle="tooltip" data-bs-custom-class="custom-tooltip">PR <span class="valor"></span></div>
    <div class="REPcampo" title="Reputación" data-bs-toggle="tooltip" data-bs-custom-class="custom-tooltip">REP <span class="valor"></span></div>
  </div>
  <div class="atrbar">
    <div class="atr vida">
      <dfn><i class="game-icon game-icon-heart-plus"></i> Vida</dfn>
      <span class="valor"></span></div>
    <div class="atr energia">
       <dfn><i class="fi fi-rr-bolt"></i> Energía</dfn>
      <span class="valor"></span></div>
    <div class="atr fuerza">
      <dfn><i class="fi fi-rr-heart-health-muscle"></i> Fuerza </dfn>
      <span class="valor"></span></div>
    <div class="atr resistencia">
      <dfn><i class="game-icon game-icon-chest-armor"></i> Resistencia </dfn>
      <span class="valor"></span></div>
    <div class="atr velocidad">
      <dfn><i class="game-icon game-icon-running-ninja"></i> Velocidad </dfn>
      <span class="valor"></span></div>
    <div class="atr inteligencia">
      <dfn><i class="game-icon game-icon-brain"></i> Inteligencia </dfn>
      <span class="valor"></span></div>
  </div>
</div>
<div class="secc">
  <div class="objsecc">
    <dfn><i class="game-icon game-icon-backpack"></i> Objetos</dfn>
    <span class="valor"></span></div>
  <div class="seccup">
    <dfn><i class="game-icon game-icon-ticket"></i> Cuponera</dfn>
    <span class="valor"></span></div>
</div>
<div class="seco">
  <div class="cronos">
    <dfn><i class="game-icon game-icon-extra-time"></i> Cronología</dfn>
    <span class="valor"></span></div>
</div>
</div>
		<!-- BEGIN switch_auth_user -->
		<dl class="left-box details" style="width: 80%;margin-top:10px;margin-bottom:10px">
			<dt>{L_ADMINISTRATE_USER}:</dt><dd><strong>{ADMINISTRATE_USER}{BAN_USER}</strong></dd>
		</dl>
		<!-- END switch_auth_user -->
<!--CAMPOS OCULTOS -->
<div id="camposreales">

<!-- BEGIN profile_field -->
<dl id="field_id{profile_field.ID}" class="left-box details">
    <dt>{profile_field.LABEL}</dt>
    <dd>{profile_field.CONTENT}</dd>
</dl>
<!-- END profile_field -->
  
<!-- BEGIN switch_awards -->
		<div class="awards_block_simple_wrapper" style="float: left; width: 100%;">
			<div class="h3">{switch_awards.L_AWARDS}</div>
			<div class="awards_block_simple">{switch_awards.AWARDS_LIST}</div>
		</div>
<!-- END switch_awards -->
  
</div>
<!-- FIN CAMPOS OCULTOS-->

<script src="{JQUERY_ROOT}json/jquery.json-1.3.min.js" type="text/javascript"></script>
<script type="text/javascript">
//<![CDATA[
	$(document).ready(function(){
		$('[id^=field_id]').each(function(){
			if ( $(this).find('.field_editable').is('span, div') )
			{
				$(this).hover(function()
				{
					if( $(this).find('.field_editable.invisible').is('span, div') )
					{
						$(this).find('.field_editable').prev().addClass('ajax-profil_hover').parent().addClass('ajax-profil_parent').append('<div class="ajax-profil_edit"><img src="{AJAX_EDIT_IMG}" /></div>');
						$(this).find('.ajax-profil_edit').attr({
								alt: "{L_FIELD_EDIT_VALUE}",
								title: "{L_FIELD_EDIT_VALUE}"
							}).click(function(){
							$(this).prev().prev().removeClass('ajax-profil_hover').addClass('invisible').next().removeClass('invisible').append('<img src="{AJAX_VALID_IMG}" class="ajax-profil_valid" />').find('input,select');
							$(this).prev().find('.ajax-profil_valid').attr({
								alt: "{L_VALIDATE}",
								title: "{L_VALIDATE}"
							}).click(function(){
								var content = new Array();
								$(this).parent().find('[name]').each(function(){
									var type_special = $(this).is('input[type=radio],input[type=checkbox]');
									if ( (type_special && $(this).is(':checked')) || !type_special )
									{
										content.push(new Array($(this).attr('name'), $(this).attr('value')));
									}
								});
								var id_name = $(this).parents('[id^=field_id]').attr('id');
								var id = id_name.substring(8, id_name.length);
								$.post(
									"{U_AJAX_PROFILE}",
									{id:id,user:"{CUR_USER_ID}",active:"{CUR_USER_ACTIVE}",content:$.toJSON(content),tid:"{TID}"},
									function(data){
										$.each(data, function (i, item) {
											if (item.startsWith('error : ')){
												let present_err=document.getElementById('field_id' + i + "_err");
												if(!present_err){
													let err = document.createElement('div');
													err.id = 'field_id' + i + "_err";
													err.classList.add("form-error");
													err.textContent = item.substring(8);
													document.getElementById('field_id' + i).querySelector('.field_editable input').after(err);
												}else{
													present_err.innerText=item.substring(8);
												}
											}else {
												let err=document.getElementById('field_id' + i + "_err");
												if(err){
													err.remove();
												}
												$('[id=field_id' + i + ']').find('.field_uneditable').html(item).end().find('.ajax-profil_valid').remove().end().find('.field_editable').addClass('invisible').end().find('.field_uneditable').removeClass('invisible');
											}
										});
									},
									"json"
								);
							});
							$(this).remove();
						});
					}
				},function()
				{
					if( $(this).find('.field_editable.invisible').is('span, div') )
					{
						$(this).find('.field_editable').prev().removeClass('ajax-profil_hover');
						$(this).find('.ajax-profil_edit').remove();
					}
				});
			}
		});
	});
//]]>
</script>
<style>
#camposreales{
position:absolute;
left:-9999px;
top:0;
width:1px;
height:1px;
overflow:hidden;
opacity:0;
}
  
.opp-perf {
    width: 100%;
    height: 100%;
    display: flex; 
    align-items: flex-start;
    flex-flow: column;
    justify-content: space-between;
    background-color: var(--color-1);
    border-radius: 0.38em;
    gap: 10px;
    color: var(--color-6);
}

.secu {
    display: flex;
    width: -webkit-fill-available;
    height: auto;
    justify-content: left;
    flex-flow: column;
    background: transparent;
}

.u-fr * {
    display: flex;
    justify-content: space-between;
    align-items: center;
}
  
.frbien {
    background: var(--color-5);
    margin: 10px;
    padding: 10px;
    border-radius: 0.18em;
}
  
.frbien i, .frname i {
    color: var(--color-1);
    background: var(--color-3);
    padding: 10px;
    border-radius: 0.18em;
    margin: 0 10px 0 0px;
}
  
.frbien h1, .frname h1 {
    font-size: 2em;
    text-transform: uppercase;
    letter-spacing: 0px;
    font-family: var(--titulos);
    color: var(--color-3);
    margin: 0;
    line-height: 1em;
}
  
.frname {
    flex-flow: row-reverse;
    background: var(--color-5);
    padding: 10px;
    margin: 10px;
    border-radius: 0.18em;
    width: max-content;
    flex: 1;
    display: flex;
    align-items: center;
    justify-content: space-between;
}
  
.frname i {
    margin: 0 0px 0 10px;
}

.tripart {
    display: flex;
    align-items: center;
    justify-content: space-between;
    margin: 10px;
}
  
.tripm {
    background: var(--color-5);
    padding: 5px 10px;
    border-radius: 0.18em;
    border: 1px solid var(--color-3);
    min-width: 225px;
    text-align: center;
    font-size: 1em;
    line-height: 1.5em;
    font-family: var(--cuerpo1);
    display: flex;
    align-items: center;
    justify-content: space-between;
}
  
.tripm.on {
    background: var(--bs-success-border-subtle);
    border: 1px solid var(--bs-form-valid-color);
}
  
.tripm.off  {
    background: var(--bs-danger-border-subtle);
    border: 1px solid var(--bs-danger-text-emphasis);
}  
  
.oscuro .tripm.off {
    background: var(--bs-danger-text-emphasis);
    border: 1px solid var(--color-3);
}  
  
.oscuro .tripm.on {
    background: var(--bs-form-valid-color);
    border: 1px solid var(--bs-success);
}  
  

.secd {
    display: flex;
    align-items: flex-start;
    justify-content: flex-start;
    width: -webkit-fill-available;
    margin: 0 10px 10px 10px;
    background: transparent;
    border-radius: 0.18em;
    overflow: hidden;
    max-height: 525px;
}
  
.secava {
    padding: 10px;
    border: 1px solid var(--color-3);
    border-radius: 0.38em;
    background: var(--color-5);
}
  
.secd .perfilavatar {
    overflow: hidden;
    border-radius: 0.18em;
}
  
.secdos {
    width: 100%;
    margin: 0 10px;
    overflow: hidden;
    height: 100%;
    display: flex;
    flex-direction: column;
    gap: 10px;
}

.secdos > .datgen {
    flex: 1;
    min-height: 0;
    overflow: auto;
    background: transparent;
}
  
.datgen h1 {
    font-size: 1em;
    font-family: var(--titulos);
    text-transform: uppercase;
    background: var(--color-5);
    padding: 10px;
    color: var(--color-3);
    margin: 0 0 10px 0;
    display: flex;
    align-items: center;
    justify-content: space-between;
}

.facgrup {
    background: var(--color-5);
    padding: 15px;
    border-radius: .45em;
    display: grid;
    grid-template-columns: 54px 1fr;
    grid-template-rows: auto auto;
    gap: 5px 15px;
    min-height: 90px;
    align-items: center;
}

.iconofac {
    grid-column: 1;
    grid-row: 1 / 3;
    display: flex;
    align-items: center;
    justify-content: center;
    background: var(--color-3);
    border-radius: .35em;
    font-size: 1.7em;
    color: var(--color-1);
    height: 50px;
}

.nombrefac{
    grid-column:2;
    grid-row:1;

    font-size:1.15em;
    font-family:var(--titulos);
    text-transform:uppercase;
    letter-spacing:.08em;
    font-weight:700;
    color:var(--color-3);

    border-bottom:1px solid var(--color-3);
    padding-bottom:4px;
}

.rangofaccion{
    grid-column:2;
    grid-row:2;
    font-size:.95em;
    font-style:italic;
    font-family:var(--cuerpo1);
    font-weight:500;
    opacity:.85;
    align-self:start;
}
  
.tdatge {
    display: flex;
    flex-direction: column;
    gap: 10px;
}

.tdatge > div{
    background:var(--color-5);
    padding:7px 10px;
    border-radius:.18em;
    display:flex;
    align-items:center;
    gap:10px;
}

.tdatge dfn {
    color: var(--color-3);
    font-size: .9em;
    font-style: normal;
    letter-spacing: 1px;
    font-weight: 600;
    font-family: var(--cuerpo1);
    text-transform: uppercase;
    flex: 0 0 auto;
    white-space: nowrap;
    display: flex;
}
  
.tdatge dfn i {
    margin: 0px 10px 0 0;
    font-size: 1.1em;
    line-height: 1em;
    display: flex;
    align-items: center;
    justify-content: center;
}  
  
.tdatge .valor{
    flex:1;           
    min-width:0;       
    text-align:right;
    line-height:1.5em;
    font-size:1em;
    font-family:var(--cuerpo1);
    letter-spacing:1px;

    overflow:hidden;
    text-overflow:ellipsis;
    white-space:nowrap;
}
  
.sectres {
    height: 100%;
    display: flex;
    flex-direction: column;
    justify-content: flex-start;
    gap: 10px;
}
  
.fra {
    overflow: hidden;
    display: flex;
    align-items: center;
    flex-flow: column;
    background: transparent;
    width: stretch;
    height: stretch;
}
  
.fra dfn {
    width: -webkit-fill-available;
    height: auto;
    padding: 10px;
    background: var(--color-5);
    display: flex;
    align-items: center;
    justify-content: space-between;
    border-radius: 0.38em;
    text-transform: uppercase;
    font-style: normal;
    font-family: var(--titulos);
    letter-spacing: 0.1em;
    color: var(--color-3);
    line-height: 1em;
    font-size: 1.2em;
}
  
.fra span {
    background: var(--color-5);
    margin: 10px 0 0;
    padding: 10px;
    border-radius: 0.38em;
    font-size: 1em;
    line-height: 1.5em;
    text-align: justify;
    font-style: normal;
    color: var(--color-6);
    overflow: auto;
    height: stretch;
}
  
.fralink {
    width: -webkit-fill-available;
    height: auto;
    background: transparent;
    display: flex;
    align-items: center;
    flex-flow: column;
}  
  
.fralink h1{
    background: var(--color-5);
    padding: 10px;
    border-radius: 0.38em;
    font-size: 1em;
    width: -webkit-fill-available;
    font-family: var(--titulos);
    text-transform: uppercase;
    color: var(--color-3);
    letter-spacing: 1px;
   display: flex; 
  align-items: center; 
  justify-content: space-between;
}  
  
.blinnky {
    width: -webkit-fill-available;
    height: auto;
    background: var(--color-5);
    padding: 10px;
    display: flex;
    align-items: center;
    justify-content: center;
    border-radius: 0.38em;
}
  
  
.blinnky > div, .blinnky > li, #field_id, #field_id-10, #field_id22  {
    display: flex;
    width: auto !important;
    margin: 0;
    padding: 0;
    position: relative;
    max-height: 40px;
    margin: 0px;
}

.blinnky > *:nth-child(2){
    display:none !important;
}  
  
.blinnky a{
    background:transparent;
    width:-webkit-fill-available;
    display:flex;
    align-items:center;
    justify-content:center;
    border-radius:0.18em;
    margin:7px 14px;
    transition:all 300ms ease;
    position:relative;
}

.blinnky a i{
    color:var(--color-3);
    font-size:1.2em;
    transition:all 300ms ease;
}

/* línea decorativa */
.blinnky a::after{
    content:"";
    position:absolute;
    bottom:-5px;
    left:50%;
    transform:translateX(-50%);
    width:0%;
    height:1px;
    background:var(--color-2);
    opacity:.8;
    transition:all 300ms ease;
}

/* hover */
.blinnky a:hover{
    transform:translateY(-4px);
}

.blinnky a:hover i{
    transform:scale(1.15);
    color:var(--color-2);
    text-shadow:
        0 0 6px color-mix(in srgb, var(--color-2) 45%, transparent),
        0 0 12px color-mix(in srgb, var(--color-2) 20%, transparent);
}

.blinnky a:hover::after{
    width:70%;
}
  
/*Playlist*/
.music {
    height: stretch;
    width: -webkit-fill-available;
    background: transparent;
    display: flex;
    align-items: center;
    justify-content: space-between;
    flex-flow: column;
    border-radius: 10px;
}
  
.music dfn {
    width: -webkit-fill-available;
    justify-content: space-between;
    display: flex;
    height: auto;
    padding: 10px;
    background: var(--color-5);
    border-radius: 0.38em;
    font-size: 1em;
    font-family: var(--titulos);
    font-style: normal;
    text-transform: uppercase;
    letter-spacing: 1px;
    color: var(--color-3);
}  
  
.music span {
    height: stretch;
    margin: 10px 0 0 0;
    background: var(--color-5);
    border-radius: 0.38em;
    display: flex;
    min-width: 200px;
    flex-flow: column;
    align-items: center;
    width: stretch;
    padding: 10px;
    justify-content: space-between;
    min-height: 170px;
}
  
.radio-retro {
    width: 100%;
    height: 100%;
}
  
.radio-cover {
    overflow: hidden;
    height: 75px;
    width: -webkit-fill-available;
    object-fit: cover;
    background: var(--color-3);
    display: flex;
    align-items: end;
    position: relative;
    border-top-left-radius: 0.18em;
    border-top-right-radius: 0.18em;
}
  
.cover-img {
    border-top-left-radius: 0.38em;
    border-top-right-radius: 0.38em;
    width: 236px;
    height: 75px;
    object-fit: cover;
}
  
.radio-title {
    background: var(--color-3);
    width: -webkit-fill-available;
    padding: 10px;
    line-height: 1em;
    font-size: 0.55em;
    border-bottom-left-radius: 10px;
    border-bottom-right-radius: 10px;
    position: relative;
    color: var(--color-1);
    margin: 0 0px 10px 0;
}
  
.radio-controls {
    display: flex;
    margin: 13px 5px 7px;
    align-items: center;
    justify-content: space-between;
    width: -webkit-fill-available;
}
  
.radio-controls button {
    padding: 10px;
    background: transparent;
    color: var(--color-3);
    border: 1px solid var(--color-3);
    border-radius: 6px;
    font-size: 12px;
} 
  
.radio-volume{
position:relative;
}

.vol-popup{
position:absolute;
bottom:45px;
left:50%;
transform:translateX(-50%);
width:40px;
height:130px;
display:none;
justify-content:center;
align-items:center;
padding:10px;
border-radius:12px;
background:rgba(0,0,0,.85);
z-index:999;
}

.volumen{
-webkit-appearance: slider-vertical;
appearance: slider-vertical;
width:30px;
height:110px;
}
 

/* BARRA ACTIVA */
.radio-progress{
width: stretch;
height: 8px;
margin: 10px;
border-radius: 20px;
overflow: hidden;
position: absolute;

background:var(--tcolor6);

box-shadow:
inset 0 0 8px rgba(255,255,255,.04),
0 0 8px rgba(0,0,0,.28);
}


/* BARRA ACTIVA */
.radio-progress .bar{
height:100%;
width:0%;
border-radius:20px;
position:relative;
transition:width .25s linear;

background:linear-gradient(
90deg,
var(--color-2),
var(--color-3),
var(--color-1),
var(--color-3),
var(--color-2)
);

background-size:300% 100%;

animation:
flowBar 4s linear infinite,
pulseGlow 2s ease-in-out infinite;
}


/* DESTELLO */
.radio-progress .bar::after{
content:"";
position:absolute;
top:0;
left:-45px;
width:45px;
height:100%;

background:linear-gradient(
90deg,
transparent,
var(--tcolor5),
transparent
);

transform:skewX(-25deg);

animation:shineRun 1.8s linear infinite;
}


/* =========================
   ANIMACIONES
   ========================= */

@keyframes flowBar{
0%{
background-position:0% 0;
}
100%{
background-position:300% 0;
}
}

@keyframes shineRun{
0%{
left:-45px;
}
100%{
left:110%;
}
}

@keyframes pulseGlow{

0%,100%{
box-shadow:
0 0 6px var(--tcolor3),
0 0 12px var(--tcolor6);
}

50%{
box-shadow:
0 0 10px var(--color-2),
0 0 20px var(--color-3),
0 0 28px var(--tcolor3);
}

}
  
  /*PARTE BELICA*/  
.sect {
    width: -webkit-fill-available;
    height: -webkit-fill-available;
    background: transparent;
    margin: 0 10px;
}
  
.belpart {
    width: -webkit-fill-available;
    display: flex;
    align-items: center;
    justify-content: space-between;
    background: var(--color-5);
    padding: 10px;
    border-radius: 0.38em;
    margin: 0 0 10px 0;
}
  
.belpart h1 {
    background: var(--color-1);
    border-radius: 0.38em;
    padding: 10px;
    display: flex;
    align-items: center;
    justify-content: flex-start;
    min-width: auto;
    color: var(--color-3);
    font-family: var(--titulos);
    text-transform: uppercase;
    font-size: 2em;
    margin: 0;
}
  
.belpart h1 i{
    margin: 0 10px 0 0px;
    color: var(--color-3);
}  
  
.bellvl {
    background: var(--color-1);
    padding: 10px;
    border-radius: 0.18em;
    font-size: 1.5em;
    font-family: var(--titulos);
    justify-content: space-between;
    display: flex;
    min-width: 200px;
    color: var(--color-3);
}
  
.pponter {
    display: flex;
    align-items: center;
    justify-content: space-between;
    background: var(--color-5);
    padding: 10px;
    border-radius: 0.38em;
}  

.pponter .PAcampo, .pponter .PPcampo, .pponter .PRcampo, .pponter .REPcampo {
    background: var(--color-1);
    padding: 5px 10px;
    min-width: 200px;
    width: auto;
    border-radius: 0.18em;
    display: flex;
    align-items: center;
    justify-content: space-between;
    color: var(--color-6);
    font-size: 1em;
    line-height: 1.5em;
    font-family: var(--cuerpo1);
    border: 1px solid var(--color-3);
}
  
.atrbar {
    width: -webkit-fill-available;
    margin: 10px 0;
    background: var(--color-5);
    border-radius: 0.38em;
    padding: 10px;
}  
  
  /*BARRAS DE ATRIBUTOS*/
  
.atr{
    display:grid;
    grid-template-columns:140px 1fr 35px;
    gap:10px;
    align-items:center;
    margin-bottom:10px;
}

.atr dfn {
    font-style: normal;
    font-size: 0.8em;
    text-transform: uppercase;
    letter-spacing: 1px;
    color: var(--color-3);
    font-family: var(--titulos);
    display: flex;
    align-items: center; 
    justify-content: flex-start; 
}
  
.atr dfn i {
    color: var(--color-3);
    margin: 0 5px 0 0px;
    border-radius: 0.18em;
    display: flex;
    align-items: center;
    justify-content: center;
    line-height: 1.5em;
    font-size: 1.5em;
}  

.atr .valor {
    display: block;
    width: 100%;
    height: 20px;
    background: var(--color-1);
    border-radius: 20px;
    position: relative;
    overflow: hidden;
    font-size: 0;
    color: transparent;
}

.atr .valor::before{
    content:"";
    position:absolute;
    left:0;
    top:0;
    width:var(--w,0%);
    height:100%;
    background:var(--c,#999);
    border-radius:20px;
    transition:.5s ease;
}

.atr .num {
    font-size: 0.8em;
    font-weight: 700;
    color: var(--color-3);
    font-family: var(--cuerpo1);
    text-align: right;
    background: var(--color-1);
    padding: 5px 10px;
    display: flex;
    align-items: center;
    justify-content: center;
    border-radius: 0.38em;
    border: 1px solid var(--color-3);
    line-height: 1.5em;
}

@keyframes fillBar{
    from{width:0;}
    to{width:var(--width);}
}  
  
  /*OBJETOS Y CUPONES*/
.secc {
    width: -webkit-fill-available;
    display: flex;
    align-items: center;
    justify-content: space-between;
    background: var(--color-5);
    margin: 0 10px;
    padding: 10px;
    border-radius: 0.38em;
    gap: 10px;
}
  
.objsecc, .seccup {
    width: stretch;
    display: flex;
    flex-flow: column;
    height: stretch;
    background: var(--color-1);
    border-radius: 0.18em;
    padding: 10px;
}
  
.objsecc > dfn, .seccup > dfn {
    width: -webkit-fill-available;
    background: var(--color-5);
    padding: 10px;
    border-radius: 0.18em;
    display: flex;
    border: 1px solid var(--color-3);
    align-items: center;
    justify-content: space-between;
    font-size: 1.5em;
    font-family: var(--titulos);
    font-style: normal;
    text-transform: uppercase;
    line-height: 1.5em;
    color: var(--color-3);
    margin: 0 0 10px 0;
    flex-flow: row-reverse;
}  
  
.objsecc > span,
.seccup > span{
    width:-webkit-fill-available;
    display:grid;
    grid-template-columns:repeat(auto-fit,minmax(70px,1fr));
    justify-content:space-between;
    gap:10px;
}
  
div.award {
    width: 100%;
    aspect-ratio: 1 / 1;
    height: auto;
    border: 2px solid var(--color-3);
    background-image: var(--award-image);
    background-size: cover;
    background-position: center;
    border-radius: 5px;
    margin: 0px;
    width:70px;
    height:70px;
}
  
  /*cronología*/
.seco{
    margin: 0 10px 10px;
    padding: 10px;
    background: var(--color-5);
    border-radius: 0.38em;
    width: -webkit-fill-available;
    height: -webkit-fill-available;
}  
</style>
